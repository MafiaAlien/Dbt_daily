### Day 5 — incremental models: is_incremental(), unique_key, and the watermark boundary (2026-08-27)
- Problem: two append-only warehouse feeds — a shipment *state log* carrying more than one row per shipment, and a write-once item-line feed — folded into one incremental mart at (`ship_date`, `warehouse_code`), where three measures are header-grain and two are line-grain, a shipment whose *latest* status is `CANCELLED` disappears entirely, and the same two nightly batches must be replayed by a var so that run 1 and run 2 are both graded.
- Traps: (1) the watermark boundary, `>` where it must be `>=` — **not reached**: the submitted mart had no `is_incremental()` block at all, so no watermark existed to get wrong; neither hit nor answered, and the verdict names the missing guard but never the operator. (2) `>=` with no `unique_key`, or a `unique_key` naming a column that is not the model's grain — **hit, in its loudest form**: `unique_key=['shipment_id', 'warehouse_code']` names a column the mart does not publish, so run 2's `delete ... using` failed to bind (`Binder Error: ... does not have a column named "shipment_id"`, candidate binding `shipment_count`) and the build returned `PASS=15 ERROR=1 SKIP=10`. V4 never ran — the day's mandatory failing-test trap was pre-empted by a harder failure one layer up. The verdict does not contain the string `unique_key`, although the error message was in hand before it was written. (3) the restatement — the header feed is not one row per shipment — **caught in code**: `row_number() over (partition by shipment_id order by updated_at desc)` with `_rn = 1`, so S008/S009's batch-2 restatements supersede correctly, including S009's 13.50 → 11.75 cost correction. (4) the fan-out, header-grain sums over line-grain rows, **fifth occurrence of the escalated blindspot and the first one avoided**: `cnt_item_quantity_weight` pre-aggregates item lines to `shipment_id` before the left join, so `shipping_cost_usd` is summed once per shipment and all eight rows are exact. Structurally the same counter-measure as the reference, placed in a mart CTE rather than the intermediate model. (5) `CASE WHEN` where the spec needs `WHERE` — **caught in code**: cancelled shipments are excluded before the `group by`, so 2026-06-02 / WH-C produces no row rather than a row of zeros, and its item lines do not leak. (6) missing `is_incremental()` guard (minor, self-announcing) — **not reached in its designed shape**: the guard was absent because the filter was absent, so `{{ this }}` was never referenced and no runtime error occurred. Caught in review as the day's headline call. A latent sixth defect, not a designed trap: the cancel filter ran at feed-row grain *before* the dedup, so a shipment cancelled in a later batch would survive via its earlier row — proven on constructed input (1 shipment / 99.00 counted where the reference counts 0), invisible on this day's seeds because S003, S007 and S014 each carry exactly one row and that row is already `CANCELLED`.
- Verdict scorecard: 2 right / 2 wrong / 1 right-call-weak-mechanism / 0 not settled, and 0 of 3 reference defects claimed. Right: the missing `is_incremental()` — the single most important call of the day, made without help; the untested intermediate model → reference. Right call, wrong mechanism: the tie-breaker placement → reference, whose stated reason ("will affect is_incremental()") is not the mechanism — tie-breaker placement is independent of the guard, and the real defect is the cancel filter's grain. Wrong, both false alarms *against himself*: "reference joins in int, I joined in mart, which is not correct" — his mart join is many-to-one against pre-aggregated item totals and does not fan out, confirmed by eight exact `shipping_cost_usd` cells, so the correct call is `equivalent`; and V5 missing `is null` — V3's `not_null` on `shipment_count` already covers that hole, so the correct call is `equivalent` there too. `Reference bugs I claim: none` missed three: the reference's `schema.yml` uses the pre-1.10 generic-test spelling and emits `MissingArgumentsPropertyInGenericTestDeprecation: 9 occurrences` on every run where Neil's `arguments:` nesting runs `WARN=0` — a point where his file is more current than the reference and he did not claim it; the reference writes `notes.md` to `dbt_practice/models/day5/` instead of `days/day5/`; and its own Design notes concede that compound-key `delete+insert` leaves an orphan row for a (date, warehouse) pair that becomes entirely cancelled. `My own solution` names the guard and nothing else — not the `unique_key`, whose error message he was holding, not the `BIGINT` counts, not the filter grain. Reference: `PASS=31 WARN=0 ERROR=0` on all three runs, every cell and all seven column types exact.
- Key takeaways:
  - `is_incremental()` 是 **Jinja 宏,编译期求值**,数据库全程没参与判断 —— `{% if %}` 决定的是编译产物里**有没有那段 where 文本**,run 1 和 run 2 发给引擎的是两份不同的 SQL(所以 `grep 'ship_date >=' target/compiled/...` 是它到底生效没有的唯一硬证据,读源码不算)。四个条件全真才返回 True：目标 relation 存在、且 `relation.type == 'table'`(存的是 view 就走全量,防物化切换错配)、模型 config 是 `incremental`、且 `not should_full_refresh()`(同时看 `--full-refresh` 和模型自己的 `full_refresh: false`)。外层还有一道 `{% if not execute %}` 短路：dbt 一条命令渲染模型两遍,parse 阶段 `execute=False` 且禁止 introspective query,直接返回 False —— 所以**它只能切换 SQL 里的过滤逻辑,不能用来切换 `ref()`**,否则 DAG 在 parse 期就建错了。
  - `>` 和 `>=` 不是风格选择,它和 `unique_key` 是**互相担保**的一对:`>=` 只有在 `unique_key` 存在时才安全(重算的行被 delete+insert 替换掉),没有 key 就退化成 append、同一个 (date, warehouse) 插两遍;`>` 只有在没有 key 时才"安全",而那时它已经因为别的原因是错的。水位线那天**恰恰是还没结账的那天** —— feed guarantee 只承诺新批次不带来更早的 `ship_date`,没承诺不带来**相等**的,而相等正是要命的情况:写 `>` 会把 2026-06-03 冻结在 run 1 的值上,少一行、错两行,**六个测试全绿**。唯一抓得住它的是逐格比对 run 2,以及 `--full-refresh` 等价校验 —— 而等价校验只在模型真的有增量谓词时才有鉴别力,没有守卫的模型两边跑的是同一件事,它永远通过。
  - 增量过滤的对象是**"需要重算的键",不是"新到的行"**。谓词要加在覆盖全量历史的上游 view 上,`ship_date >= watermark` 选中一天之后,这一天要用**全部** feed 重算(06-03 的 S008 来自 batch 1、S010 来自 batch 2,必须一起进 `sum`);换成 `where batch_id = 2` 算出来的是残缺结果,再盖到旧行上,那一天就废了。配套的两件事:`max(ship_date) from {{ this }}` 外面要包 `coalesce(..., 哨兵日期)`,因为空表时 `max()` 返回 NULL 而 `ship_date >= NULL` 求值为 NULL 不是 TRUE,会安静地产出空表 —— `is_incremental()` 只保证表**存在**,不保证**非空**;`unique_key` 必须就是模型声明的 grain,写成别的列在 `delete ... using` 绑定阶段直接炸,写成 grain 的子集则会误删。
- Recurring-pattern check: one blindspot broken, two repeated, one escalated. **Broken — and this is the day's real result:** the fan-out is the **fifth** setup of `[modeling] counting at a grain that is not the entity being counted` (Day 1 null-padded row counted, Day 2 dedup side, Day 3 `subscriptions LEFT JOIN usage_events`, Day 4 `payouts LEFT JOIN payout_lines` after an explicit warning in the problem text) and **the first one he did not walk into**. The day was built so `distinct` could not rescue it — the exposed measure is `shipping_cost_usd`, a header amount that `count(distinct)` cannot fix — and the counter-measure he wrote is the structural one: aggregate the lines to shipment grain before the join. Four consecutive days of the same defect ended here. It is a *fragile* pass, though, and the fragility is worth recording: `## Grain plan` puts `shipping_cost_usd` at `(ship date, warehouse)` when it is header-grain — the one cell the trap aims at — so the written plan does not yet carry the reasoning the SQL got right. **Repeated, fifth occurrence, fourth consecutive day:** `[general] deferring a topic without extracting a one-sentence summary` — `days/day5/notes.md` has `## Assumptions`, `## Run log` and `## Debrief answers` empty, all four debrief questions unanswered, and this time the filled sections have gone *stale rather than blank*: `## Config decisions` still says "int table uses ephemeral" after the model became a view, `## Grain plan` still cites `int_d5_filter_cancelled`, a model that no longer exists, and neither ever answered the problem's explicit question — name the adapter's default `incremental_strategy` and say how you found it. Confirmed from the run artifact this day: `delete+insert`. **Repeated:** `[dbt] reviewing one's own solution without comparing outputs` — fifth occurrence, with the failure mode *inverted*. Days 1–4 under-examined his own code; Day 5 over-conceded, judging himself wrong twice where the two versions are equivalent, while the one defect that actually broke his build — a `unique_key` naming a column the model does not publish — is absent from a verdict written with the error message already in hand. Reading the reference's shape and inferring "mine must be worse" produces the same empty result as not reading at all. **Escalated, second occurrence:** `[process] modifying the graded artifact after seeing execution results`, first named Day 4. `models/day5/marts/fct_d5_daily_warehouse_shipments.sql` was rewritten at 21:17, after `verdict.md` was committed at `45c8b52` and while `/review` phase 2 was transcribing the reference, changing `unique_key` from `shipment_id` to `ship_date`; `models/day5/` was untracked in git throughout, so once again no pristine copy existed and the grade rests on the phase-1 file dump. Day 4's cheap fix — commit the day's models before `/review` runs — was available and was not used. **Not repeated:** the Day 4 type drift was reproduced (`shipment_count` and `delivered_count` landed as `BIGINT` against a contract of `integer`) but corrected in the post-review practice pass, along with the guard, the filter grain, and the intermediate model's NULL leak; the rebuilt solution runs `PASS=33 WARN=0 ERROR=0` on all three runs with all seven types exact and the equivalence check meaningful for the first time.
- Parking lot additions: dbt **model versioning** (`versions:` / `latest_version:` in a model's properties block, dbt ≥1.5) — raised while disambiguating the three different `version:` keys; it publishes several versions of one model (`my_model_v1`, `my_model_v2`) so downstream consumers can migrate off a breaking change on their own schedule, and it is unrelated to the `version: 2` file-format key or to `dbt_project.yml`'s project version. Not a deferred topic but an unwritten deliverable: all four debrief questions, of which Q2 (construct the batch where `unique_key='ship_date'` and the compound key diverge, and name the `incremental_strategy` assumed) and Q3 (name the line that depends on the feed guarantee, and the pattern that survives the guarantee breaking) are the two whose answers this day's execution never forced him to find.
